export class Friend {
  id: number;
  name: string;
  isFriend: boolean;

  constructor(id: number, name: string, isFriend: boolean) {
    this.id = id;
    this.name = name;
    this.isFriend = isFriend;
  }
}
